# Secrets — never commit
.env
.env.*
!.env.example

# Go build output
bench-cli.exe
bench-cli
*.exe

# Local bench data
claimsBench/data/

# Planning notes — kept locally, not part of the deliverable
claude docs/
