1. credibility score generator with expiry + acc id and domain + revoke if fraud (VERY OPTIONAL)

basically this agent makes the merchant transactable 100% through ai end to end 
so current agent problems 

2. if a user has had a converstation with my voice agent, I'll ofc store these *user consent needed*
		Then it has episodic memories but over time I'll also store them into semantic memory for easy access and this goes into my harness 
	