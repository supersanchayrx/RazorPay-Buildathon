node_modules

# macOS
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

.env
.env.*
# ...but the template must ship, or a fresh clone cannot start.
!.env.example





/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Hide files auto-generated by react router
.react-router/
# Generated at runtime or by `npm run seed`. Regenerable, and the seed is
# deterministic, so committing a thousand JSONL rows only adds review noise.
/data/
decision-ledger.jsonl
